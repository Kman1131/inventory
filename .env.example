API_KEY=inventory-secret-key

# Optional host ports if 3000 or 8080 are already occupied.
BACKEND_HOST_PORT=3000
WEB_HOST_PORT=8080

# IPv4 address of the machine running Docker on your local network.
# Used by the Expo container so your phone can reach the dev server.
LAN_IP=192.168.1.100